window.ArtsList=[{date:"2025-09",files:["arts/arts-all.min.js"]},{date:"2025-08",files:["arts/arts-all.min.js"]}];window.ArtsLists=[];window.getArtsFilesList=function(){return window.ArtsList};window.getAllArtsFiles=function(){const allFiles=[];window.ArtsList.forEach(dateGroup=>{dateGroup.files.forEach(file=>{allFiles.push(file)})});return allFiles};window.getAllArtsSorted=function(){const allArts=[];if(window.ArtsLoader&&window.ArtsLoader.loadAllArts){const loaderArts=window.ArtsLoader.loadAllArts();allArts.push(...loaderArts)}const artsByDate={};allArts.forEach(art=>{const dateMatch=art.id.match(/art_(\d{4}_\d{2})_/);if(dateMatch){const date=dateMatch[1].replace("_","-");if(!artsByDate[date]){artsByDate[date]=[]}artsByDate[date].push(art)}});Object.keys(artsByDate).forEach(date=>{artsByDate[date].sort((a,b)=>{const aMatch=a.id.match(/art_\d{4}_\d{2}_(\d+)/);const bMatch=b.id.match(/art_\d{4}_\d{2}_(\d+)/);if(aMatch&&bMatch){const aNum=parseInt(aMatch[1]);const bNum=parseInt(bMatch[1]);return bNum-aNum}return 0})});const sortedDates=Object.keys(artsByDate).sort((a,b)=>b.localeCompare(a));const sortedArts=[];sortedDates.forEach(date=>{sortedArts.push(...artsByDate[date])});return sortedArts};window.getArtsByDateSorted=function(){const allArts=[];if(window.ArtsLoader&&window.ArtsLoader.loadAllArts){const loaderArts=window.ArtsLoader.loadAllArts();allArts.push(...loaderArts)}const artsByDate={};allArts.forEach(art=>{const dateMatch=art.id.match(/art_(\d{4}_\d{2})_/);if(dateMatch){const date=dateMatch[1].replace("_","-");if(!artsByDate[date]){artsByDate[date]=[]}artsByDate[date].push(art)}});Object.keys(artsByDate).forEach(date=>{artsByDate[date].sort((a,b)=>{const aMatch=a.id.match(/art_\d{4}_\d{2}_(\d+)/);const bMatch=b.id.match(/art_\d{4}_\d{2}_(\d+)/);if(aMatch&&bMatch){const aNum=parseInt(aMatch[1]);const bNum=parseInt(bMatch[1]);return bNum-aNum}return 0})});const sortedDates=Object.keys(artsByDate).sort((a,b)=>b.localeCompare(a));const result=sortedDates.map(date=>({date:date,arts:artsByDate[date]}));return result};